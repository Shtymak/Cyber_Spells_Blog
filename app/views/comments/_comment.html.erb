<div id="comment_<%=comment.id%>">
<p><%=comment.user.email%> <%=link_to "Delete", comment_path(comment), method: :delete, :class =>"btn btn-outline-danger" if comment.user_id == current_user.id%></p>
<p>
<%=comment.body%>
</p>
<%=comment.created_at%>
</div>